const { aaron, marion, alliana } = require("./names");
const { sayHi } = require("./utils");
const data = require("./alternative-flavor");

require('./mind-grenade')
// sayHi(aaron);
// sayHi(marion);
// sayHi(alliana);

// console.log(data);
